# Copy this file to ".env" (in the same folder) and fill in your own values.
#
# These variables are used by StudyBuddy AI for Gemini and (optionally)
# Google Cloud / Vertex AI + ADK configuration.

# REQUIRED: API key for Gemini (google-generativeai or ADK).
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# OPTIONAL: Override the default model (see config/settings.py).
# Recommended models with stable function calling support:
# - gemini-2.0-flash-001 (latest stable with function calling)
# - gemini-1.5-flash (proven stable)
# - gemini-1.5-pro (more capable, also stable)
# Note: gemini-2.0-flash-exp may not support function calling with google_search
GEMINI_MODEL=gemini-2.0-flash-001

# OPTIONAL: Project and Vertex AI configuration, if you use Vertex-backed Gemini.
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_GENAI_USE_VERTEXAI=false

# OPTIONAL: Any other environment values you want to pass into agents/tools.
# MAX_QUIZ_QUESTIONS=10
# SESSION_TIMEOUT_MINUTES=30
# MEMORY_RETENTION_DAYS=90


